apiVersion: v1
kind: Service
metadata:
  name: gov-benefits-graphql
  namespace: minikube-demo
  labels:
    app: gov-benefits-graphql
    lab: government-services
spec:
  type: ClusterIP
  selector:
    app: gov-benefits-graphql
  ports:
    - name: http
      port: 8080
      targetPort: 8080
